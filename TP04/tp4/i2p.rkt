#lang racket
(displayln "nom1, prénom1")
(displayln "nom2, prénom2")

(require (file "commun.rkt")) 
(provide i->p)

#|Évidemment, vous devez remplacer les noms fictifs par vos noms. 
  L'exécution de vos fichiers ne doit pas retourner de valeur
  ou afficher des messages, i.e. enlever tous les messages
  ou tests que vous avez inclus dans votre fichier.
|#

(define i->p
  (λ (e)
    
    ))
  